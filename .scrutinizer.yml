#tools:
#    php_sim: true
#    php_pdepend: true
#    php_analyzer: true
#    external_code_coverage:
#        timeout: 1200    # Timeout in seconds.    
#        runs: 3
build:
  nodes:
    analysis:
      tests:
        override:
          - php-scrutinizer-run
          - phpcs-run

filter:
    excluded_paths: [bin/*, tests/*]

checks:
    php: true